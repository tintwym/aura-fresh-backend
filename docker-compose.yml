# Aura Fresh API — Docker + Neon PostgreSQL
#
# Prerequisites:
#   1. Copy .env.example → .env
#   2. Set DATABASE_URL to your Neon connection string (sslmode=require)
#   3. Fill JWT_SECRET and other keys
#
# Run:
#   docker compose --env-file .env up --build
#
# API: http://localhost:8080/api

name: aura_fresh_backend

services:
  api:
    build: .
    image: aura_fresh_backend-api:latest
    container_name: aura-fresh-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      # Neon — required (from .env). Do not run a local Postgres container.
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env to your Neon postgresql:// URL}
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env (min 32 characters)}
      STRIPE_API_KEY: ${STRIPE_API_KEY:-sk_test_placeholder}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      APP_FRONTEND_BASE_URL: ${APP_FRONTEND_BASE_URL:-http://localhost:3000}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME:-}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY:-}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET:-}
      CLOUDINARY_FOLDER: ${CLOUDINARY_FOLDER:-aura-fresh/products}
      ADMIN_SEED_USERNAME: ${ADMIN_SEED_USERNAME:-}
      ADMIN_SEED_PASSWORD: ${ADMIN_SEED_PASSWORD:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_IDS: ${GOOGLE_CLIENT_IDS:-}
      APPLE_CLIENT_ID: ${APPLE_CLIENT_ID:-}
      APPLE_CLIENT_IDS: ${APPLE_CLIENT_IDS:-}
      PORT: "8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    restart: unless-stopped
